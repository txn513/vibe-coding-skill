# 数据库规范

## 命名
- 表名: 小写蛇形，复数 `users`, `order_items`
- 字段: 小写蛇形 `created_at`, `updated_by`
- 索引: `idx_表名_字段`

## 迁移
- 每次变更一个独立迁移文件
- 迁移必须可回滚
- 不删字段，用软删除或标记废弃

## 查询
- 避免 `SELECT *`
- 大表查询必须有索引覆盖
- 批量操作分页处理
